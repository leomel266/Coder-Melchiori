import React from 'react';
import cart from './img/cart.png'

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt='cart'/>
        </div>
    );
}

export default CartWidget;
